"""
Settings which are not to go inside version control.
That is, passwords, secret keys, etc.
Feel free to override any setting in here though, e.g. "DEBUG = True".
"""

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = ''

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

TEMPLATE_DEBUG = True

# Database
# https://docs.djangoproject.com/en/1.6/ref/settings/#databases

DATABASES = {
	'default': {
    		'ENGINE': 'django.db.backends.postgresql_psycopg2',
    		'NAME': 'django',
    		'USER': '',
    		'PASSWORD': '',
    		'HOST': 'localhost,
    		'PORT': '5432'
	},
	'gov2014db': {
    		'ENGINE': 'django.db.backends.postgresql_psycopg2',
    		'NAME': 'gov2014db',
    		'USER': '',
    		'PASSWORD': '',
    		'HOST': 'localhost',
    		'PORT': '5432'
	}
}

# Including port number (if not port 80) and path.
# In the future, vagrant should set this up so that it is on the same host,
# and using port 80. In that case, you could just specify a relative path
# for this value. But for now, it's a little bit funky.
GEOSERVER_URL = 'http://localhost:18080/geoserver'
